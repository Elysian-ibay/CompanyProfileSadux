# Frontend production env (Vercel).
# Set VITE_API_URL to your deployed backend URL + /api.
# You can set this here OR (recommended) as an Environment Variable in the
# Vercel dashboard for the frontend project. It is read at BUILD time.
VITE_API_URL=https://your-backend.vercel.app/api
